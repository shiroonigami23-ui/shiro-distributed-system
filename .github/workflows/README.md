# .github/workflows

GitHub Actions workflows for build/test automation.

- `ci.yml`: baseline build + test
- `hardening-ci.yml`: vet + race tests + vulnerability scan
- `checklist-gate.yml`: validates 100-point hardening checklist integrity

